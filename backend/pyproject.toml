[project]
name = "yzu-student-assistant-api"
version = "0.1.0"
description = "Local API for the YZU Student Assistant"
requires-python = ">=3.13,<3.14"
dependencies = [
    "fastapi==0.141.1",
    "uvicorn==0.53.0",
]

[tool.uv]
package = false
